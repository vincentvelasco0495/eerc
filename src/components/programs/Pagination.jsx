/**
 * @deprecated Import from `src/components/server-pagination` instead.
 * These names are kept for backward compatibility with dashboard program/enrollment pages.
 */
export {
  ServerListPerPageControl as ProgramsPerPageControl,
  ServerListPagination as ProgramsPagination,
} from 'src/components/server-pagination';
