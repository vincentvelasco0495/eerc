export const styles = {
  periodSelect: {
    minWidth: { xs: 0, sm: 132 },
    width: { xs: 1, sm: 'auto' },
  },
  periodMenuPaper: {
    maxHeight: 320,
    maxWidth: 'min(calc(100vw - 24px), 360px)',
  },
  toolbarButton: {
    width: { xs: 1, sm: 'auto' },
    justifyContent: { xs: 'center', sm: 'flex-start' },
  },
};
